{
  "name": "open-weather-command-center",
  "version": "0.1.0-beta.1",
  "description": "Open Weather Station Command Center — cross-platform Electron dashboard",
  "main": "./out/main/index.js",
  "author": "Nicholas Tracy <nicholas.tracy@outlook.com>",
  "license": "GPL-3.0-or-later",
  "homepage": "https://github.com/NicholasTracy/Open-Weather",
  "scripts": {
    "dev": "electron-vite dev",
    "dev:agent": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "pack": "npm run build && electron-builder --dir --publish never",
    "dist": "npm run build && electron-builder --publish never",
    "dist:win": "npm run build && electron-builder --win nsis --x64 --publish never",
    "dist:mac": "npm run build && electron-builder --mac dmg --publish never",
    "dist:linux": "npm run build && electron-builder --linux deb --publish never",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "agent:mcp": "node ./agent/mcp-server.mjs"
  },
  "dependencies": {
    "bootstrap": "^5.3.7",
    "leaflet": "^1.9.4",
    "nexrad-level-2-data": "^3.0.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-leaflet": "^5.0.0"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.22",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^4.6.0",
    "electron": "^43.4.1",
    "electron-builder": "^26.0.12",
    "electron-vite": "^5.0.0",
    "typescript": "^7.0.2",
    "vite": "^6.3.5"
  },
  "build": {
    "appId": "com.openweather.commandcenter",
    "productName": "Open Weather Command Center",
    "directories": {
      "output": "release",
      "buildResources": "resources"
    },
    "files": [
      "out/**/*",
      "resources/**/*"
    ],
    "asarUnpack": [
      "node_modules/@foxglove/wasm-bz2/**",
      "node_modules/nexrad-level-2-data/**"
    ],
    "mac": {
      "target": [
        "dmg",
        "zip"
      ],
      "category": "public.app-category.weather",
      "identity": null,
      "hardenedRuntime": false,
      "gatekeeperAssess": false,
      "notarize": false
    },
    "win": {
      "icon": "icon.png",
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName} ${version}.${ext}",
      "signAndEditExecutable": true,
      "signExecutable": false
    },
    "nsis": {
      "artifactName": "${productName} ${version} Setup.${ext}",
      "oneClick": false,
      "perMachine": false,
      "allowElevation": true,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Open Weather",
      "uninstallDisplayName": "Open Weather Command Center",
      "deleteAppDataOnUninstall": false,
      "license": "../LICENSE"
    },
    "portable": {
      "artifactName": "${productName} ${version} Portable.${ext}"
    },
    "linux": {
      "executableName": "open-weather-command-center",
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Science"
    }
  }
}
